apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: clubhive-ingress
  namespace: {{ .Values.namespace }}
  annotations: {}
spec:
  ingressClassName: {{ .Values.ingress.ingressClassName }}
  rules:
  - host: {{ .Values.ingress.host }}
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: ems-scraper-service
            port:
              number: 3100