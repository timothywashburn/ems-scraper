---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ems-scraper-https-ingress
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`{{ .Values.ingress.host }}`) && PathPrefix(`/api`)
      priority: 20
      services:
        - name: server-service
          port: 3100
    - kind: Rule
      match: Host(`{{ .Values.ingress.host }}`)
      priority: 10
      services:
        - name: client-service
          port: 80
  tls:
    secretName: ems-scraper-tls
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: ems-scraper-https-redirect
spec:
  redirectScheme:
    scheme: https
    permanent: true
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ems-scraper-http-ingress
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`{{ .Values.ingress.host }}`) && !PathPrefix(`/.well-known/acme-challenge/`)
      middlewares:
        - name: ems-scraper-https-redirect
      services:
        - name: client-service
          port: 80
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ems-scraper-tls
spec:
  secretName: ems-scraper-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - {{ .Values.ingress.host }}