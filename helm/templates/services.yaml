apiVersion: v1
kind: Service
metadata:
  name: ems-scraper-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: ems-scraper
  ports:
  - port: 3000
    targetPort: 3000
  type: ClusterIP