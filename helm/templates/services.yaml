apiVersion: v1
kind: Service
metadata:
  name: ems-scraper-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: ems-scraper
  ports:
  - port: 3100
    targetPort: 3100
  type: ClusterIP